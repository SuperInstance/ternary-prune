[package]
name = "ternary-prune"
version = "0.1.0"
edition = "2021"
description = "Ternary network pruning — magnitude, gradient, and structured pruning"
license = "MIT"
repository = "https://github.com/SuperInstance/ternary-prune"
